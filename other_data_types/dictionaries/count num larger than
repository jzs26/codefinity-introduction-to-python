from typing import List

class Solution(object):
    def solve(self,scores:List[int], k:int) -> int:
        print(f"scores: {scores}, k: {k}")
        count = 0
        for num in scores:
            if num > k: 
                count += 1
        return count
